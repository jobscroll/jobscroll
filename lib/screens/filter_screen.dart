// filter_screen.dart
import 'package:flutter/material.dart';

class FilterScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Filteroptionen'),
      ),
      body: Center(
        child: Text('Filteroptionen werden hier implementiert.'),
      ),
    );
  }
}
